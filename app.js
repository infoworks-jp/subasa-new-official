(()=>{const reduce=matchMedia('(prefers-reduced-motion: reduce)').matches;const els=[...document.querySelectorAll('.reveal')];if('IntersectionObserver'in window){const io=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){e.target.classList.add('in');io.unobserve(e.target)}}),{threshold:.15});els.forEach(e=>io.observe(e))}else els.forEach(e=>e.classList.add('in'));
const dialog=document.querySelector('#menuDialog'),img=document.querySelector('#menuImage'),label=document.querySelector('#menuLabel');document.querySelectorAll('[data-menu]').forEach(b=>b.addEventListener('click',()=>{img.src=b.dataset.menu;img.alt=b.dataset.label;label.textContent=b.dataset.label;dialog.showModal()}));dialog.querySelector('.close').onclick=()=>dialog.close();dialog.addEventListener('click',e=>{if(e.target===dialog)dialog.close()});
if(reduce)return;const c=document.querySelector('#ink'),x=c.getContext('2d',{alpha:true});let w,h,dpr,px=.5,py=.5,tx=.5,ty=.5,t=0;function size(){dpr=Math.min(devicePixelRatio||1,2);w=innerWidth;h=innerHeight;c.width=w*dpr;c.height=h*dpr;c.style.width=w+'px';c.style.height=h+'px';x.setTransform(dpr,0,0,dpr,0,0)}size();addEventListener('resize',size,{passive:true});addEventListener('pointermove',e=>{tx=e.clientX/w;ty=e.clientY/h},{passive:true});function blob(cx,cy,r,a,rgb){const g=x.createRadialGradient(cx,cy,0,cx,cy,r);g.addColorStop(0,`rgba(${rgb},${a})`);g.addColorStop(.35,`rgba(${rgb},${a*.55})`);g.addColorStop(1,`rgba(${rgb},0)`);x.fillStyle=g;x.beginPath();x.arc(cx,cy,r,0,Math.PI*2);x.fill()}function frame(){t+=.006;px+=(tx-px)*.028;py+=(ty-py)*.028;x.clearRect(0,0,w,h);x.globalCompositeOperation='lighter';const bx=w*(.2+.55*px),by=h*(.18+.55*py);blob(bx+Math.sin(t*8)*90,by+Math.cos(t*6)*70,Math.max(w,h)*.22,.13,'150,0,20');blob(w*(.65+.1*Math.sin(t*4)),h*(.35+.13*Math.cos(t*3)),Math.max(w,h)*.18,.07,'255,115,22');blob(w*(.35+.12*Math.cos(t*5)),h*(.65+.08*Math.sin(t*7)),Math.max(w,h)*.20,.055,'80,20,140');requestAnimationFrame(frame)}frame()})();