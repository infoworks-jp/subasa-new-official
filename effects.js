(()=>{const reduce=matchMedia('(prefers-reduced-motion: reduce)').matches;const canvases=[...document.querySelectorAll('.fx-canvas')];const states=new Map();const io='IntersectionObserver'in window?new IntersectionObserver(es=>es.forEach(e=>{const s=states.get(e.target);if(s)s.active=e.isIntersecting}),{rootMargin:'150px 0px'}):null;function fit(c){const r=c.parentElement.getBoundingClientRect(),d=Math.min(devicePixelRatio||1,1),w=Math.max(1,Math.round(r.width*d)),h=Math.max(1,Math.round(r.height*d));if(c.width!==w||c.height!==h){c.width=w;c.height=h;c.style.width=r.width+'px';c.style.height=r.height+'px'}return d}function ppos(c,e){const r=c.getBoundingClientRect();return[(e.clientX-r.left)/r.width,(e.clientY-r.top)/r.height]}function steam(c){const x=c.getContext('2d',{alpha:true});let s=[],last=0;const host=c.parentElement,profile=c.dataset.profile||'miso',state={active:true};states.set(c,state);io?.observe(c);function source(){const mobile=innerWidth<700;if(profile==='miso')return mobile?{cx:.52,cy:.60,spread:.18}:{cx:.57,cy:.60,spread:.17};return mobile?{cx:.48,cy:.55,spread:.22}:{cx:.51,cy:.55,spread:.20}}host.addEventListener('pointermove',e=>{if(!state.active)return;const q=ppos(c,e);for(const a of s){const dx=a.x-q[0],dy=a.y-q[1],m=dx*dx+dy*dy;if(m<.08){a.vx+=dx*.0016;a.vy+=dy*.0003}}},{passive:true});function f(now){requestAnimationFrame(f);if(reduce||!state.active||document.hidden||now-last<33)return;last=now;const d=fit(c),w=c.width,h=c.height,src=source();x.clearRect(0,0,w,h);if(Math.random()<.55)s.push({x:src.cx+(Math.random()-.5)*src.spread,y:src.cy+Math.random()*.025,vx:(Math.random()-.5)*.00024,vy:-.00115-Math.random()*.00105,a:.09+Math.random()*.13,r:9+Math.random()*15,phase:Math.random()*6.28});x.globalCompositeOperation='screen';x.filter=`blur(${4*d}px)`;for(const a of s){a.x+=a.vx+Math.sin(now/750+a.phase+a.y*18)*.00028;a.y+=a.vy;a.a*=.992;a.r+=.038;const len=a.r*2.5*d,g=x.createLinearGradient(a.x*w,a.y*h+len*.5,a.x*w,a.y*h-len*.5);g.addColorStop(0,'rgba(255,255,255,0)');g.addColorStop(.38,`rgba(255,255,255,${a.a*.62})`);g.addColorStop(.7,`rgba(255,255,255,${a.a})`);g.addColorStop(1,'rgba(255,255,255,0)');x.strokeStyle=g;x.lineWidth=(a.r*.42+3)*d;x.lineCap='round';x.beginPath();x.moveTo(a.x*w,a.y*h+len*.26);x.bezierCurveTo((a.x-.012)*w,(a.y-.03)*h,(a.x+.014)*w,(a.y-.07)*h,(a.x+Math.sin(now/900+a.phase)*.01)*w,(a.y-.11)*h);x.stroke()}x.filter='none';s=s.filter(a=>a.y>.15&&a.a>.018).slice(-70)}requestAnimationFrame(f)}canvases.forEach(c=>{c.style.position='absolute';c.style.inset='0';c.style.zIndex='4';c.style.pointerEvents='none';if(c.dataset.effect==='steam')steam(c)});window.__tsubasaEffects={isolated:true,optimized:true,effects:canvases.map(c=>({effect:c.dataset.effect,profile:c.dataset.profile||null}))};})();