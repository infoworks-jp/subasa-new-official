(()=>{const reduce=matchMedia('(prefers-reduced-motion: reduce)').matches;const canvases=[...document.querySelectorAll('.fx-canvas')];function size(c){const r=c.parentElement.getBoundingClientRect(),d=Math.min(devicePixelRatio||1,1.5);c.width=Math.max(1,r.width*d);c.height=Math.max(1,r.height*d);c.style.width=r.width+'px';c.style.height=r.height+'px';return d}function pos(c,e){const r=c.getBoundingClientRect();return[(e.clientX-r.left)/r.width,(e.clientY-r.top)/r.height]}function ink(c){const x=c.getContext('2d');let p=[],mouse=[.5,.5];for(let i=0;i<22;i++)p.push({x:Math.random(),y:Math.random(),r:30+Math.random()*100,vx:(Math.random()-.5)*.00025,vy:(Math.random()-.5)*.00018,h:i%3});c.parentElement.addEventListener('pointermove',e=>{mouse=pos(c,e);for(let i=0;i<3;i++)p.push({x:mouse[0]+(Math.random()-.5)*.04,y:mouse[1]+(Math.random()-.5)*.04,r:35+Math.random()*65,vx:(Math.random()-.5)*.001,vy:(Math.random()-.5)*.001,h:i%2})});function f(){const d=size(c),w=c.width,h=c.height;x.clearRect(0,0,w,h);x.globalCompositeOperation='screen';x.filter=`blur(${28*d}px)`;for(const a of p){a.x+=a.vx;a.y+=a.vy;if(a.x<-.2)a.x=1.2;if(a.x>1.2)a.x=-.2;if(a.y<-.2)a.y=1.2;if(a.y>1.2)a.y=-.2;const g=x.createRadialGradient(a.x*w,a.y*h,0,a.x*w,a.y*h,a.r*d);g.addColorStop(0,a.h===0?'rgba(155,0,38,.48)':a.h===1?'rgba(16,42,120,.36)':'rgba(20,0,30,.28)');g.addColorStop(1,'rgba(0,0,0,0)');x.fillStyle=g;x.beginPath();x.arc(a.x*w,a.y*h,a.r*d,0,7);x.fill()}p=p.slice(-45);x.filter='none';if(!reduce)requestAnimationFrame(f)}f()}function steam(c){const x=c.getContext('2d');let s=[];const host=c.parentElement;host.addEventListener('pointermove',e=>{const q=pos(c,e);s.forEach(a=>{const dx=a.x-q[0],dy=a.y-q[1],m=dx*dx+dy*dy;if(m<.05)a.vx+=dx*.0008})});function f(){const d=size(c),w=c.width,h=c.height;x.clearRect(0,0,w,h);if(Math.random()<.55)s.push({x:.56+(Math.random()-.5)*.14,y:.67+Math.random()*.04,vx:(Math.random()-.5)*.00045,vy:-.0012-Math.random()*.0012,a:.18+Math.random()*.18,r:16+Math.random()*24});x.globalCompositeOperation='screen';for(const a of s){a.x+=a.vx+Math.sin(a.y*30+performance.now()/900)*.00018;a.y+=a.vy;a.a*=.994;a.r+=.06;const g=x.createRadialGradient(a.x*w,a.y*h,0,a.x*w,a.y*h,a.r*d);g.addColorStop(0,`rgba(255,255,255,${a.a})`);g.addColorStop(1,'rgba(255,255,255,0)');x.fillStyle=g;x.beginPath();x.arc(a.x*w,a.y*h,a.r*d,0,7);x.fill()}s=s.filter(a=>a.y>.18&&a.a>.025).slice(-100);if(!reduce)requestAnimationFrame(f)}f()}function ripple(c){const x=c.getContext('2d');let r=[];const host=c.parentElement;function add(e){const q=pos(c,e);r.push({x:q[0],y:q[1],rad:2,a:.42})}host.addEventListener('pointermove',e=>{if(Math.random()<.16)add(e)});host.addEventListener('pointerdown',add);function f(){const d=size(c),w=c.width,h=c.height;x.clearRect(0,0,w,h);x.globalCompositeOperation='screen';for(const a of r){a.rad+=1.15*d;a.a*=.982;x.strokeStyle=`rgba(160,205,255,${a.a})`;x.lineWidth=1.2*d;x.beginPath();x.ellipse(a.x*w,a.y*h,a.rad*1.35,a.rad*.72,0,0,7);x.stroke()}r=r.filter(a=>a.a>.025&&a.rad<Math.max(w,h)*.35);if(Math.random()<.015)r.push({x:.66+Math.random()*.12,y:.56+Math.random()*.08,rad:2,a:.2});if(!reduce)requestAnimationFrame(f)}f()}canvases.forEach(c=>{c.style.position='absolute';c.style.inset='0';c.style.zIndex='4';c.style.pointerEvents='none';if(c.dataset.effect==='ink')ink(c);if(c.dataset.effect==='steam')steam(c);if(c.dataset.effect==='ripple')ripple(c)});window.__tsubasaEffects={isolated:true,effects:canvases.map(c=>c.dataset.effect)};})();