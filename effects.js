(()=>{const reduce=matchMedia('(prefers-reduced-motion: reduce)').matches;const canvases=[...document.querySelectorAll('.fx-canvas')];function size(c){const r=c.parentElement.getBoundingClientRect(),d=Math.min(devicePixelRatio||1,1.5),w=Math.max(1,Math.round(r.width*d)),h=Math.max(1,Math.round(r.height*d));if(c.width!==w||c.height!==h){c.width=w;c.height=h;c.style.width=r.width+'px';c.style.height=r.height+'px'}return d}function pos(c,e){const r=c.getBoundingClientRect();return[(e.clientX-r.left)/r.width,(e.clientY-r.top)/r.height]}function ink(c){const x=c.getContext('2d');let drops=[];for(let i=0;i<30;i++)drops.push({x:Math.random(),y:Math.random(),r:18+Math.random()*90,dr:.08+Math.random()*.16,vx:(Math.random()-.5)*.00032,vy:(Math.random()-.5)*.00025,t:i%5===0?1:0,a:.18+Math.random()*.24});function pour(q,burst=false){const n=burst?18:6;for(let i=0;i<n;i++){const ang=Math.random()*Math.PI*2,sp=burst?.0025*Math.random():.0007*Math.random();drops.push({x:q[0]+Math.cos(ang)*Math.random()*.025,y:q[1]+Math.sin(ang)*Math.random()*.025,r:burst?5+Math.random()*30:12+Math.random()*50,dr:.16+Math.random()*.28,vx:Math.cos(ang)*sp,vy:Math.sin(ang)*sp,t:Math.random()<.18?1:0,a:burst?.62:.42})}}let down=false;c.parentElement.addEventListener('pointerdown',e=>{down=true;pour(pos(c,e),true)});addEventListener('pointerup',()=>down=false);c.parentElement.addEventListener('pointermove',e=>{if(down)pour(pos(c,e),false)});function f(){const d=size(c),w=c.width,h=c.height;x.clearRect(0,0,w,h);x.globalCompositeOperation='screen';for(const a of drops){a.x+=a.vx;a.y+=a.vy;a.r+=a.dr;a.vx*=.998;a.vy*=.998;a.a*=.9992;const rr=a.r*d,g=x.createRadialGradient(a.x*w,a.y*h,Math.max(0,rr*.16),a.x*w,a.y*h,rr);if(a.t){g.addColorStop(0,`rgba(210,18,36,${a.a*.9})`);g.addColorStop(.48,`rgba(125,0,24,${a.a*.62})`)}else{g.addColorStop(0,`rgba(8,8,12,${a.a})`);g.addColorStop(.55,`rgba(25,30,48,${a.a*.78})`)}g.addColorStop(.72,'rgba(0,0,0,0)');g.addColorStop(.82,a.t?`rgba(205,20,38,${a.a*.42})`:`rgba(30,38,62,${a.a*.42})`);g.addColorStop(1,'rgba(0,0,0,0)');x.fillStyle=g;x.beginPath();x.arc(a.x*w,a.y*h,rr,0,7);x.fill()}drops=drops.filter(a=>a.a>.025&&a.r<260).slice(-150);if(Math.random()<.035)pour([.15+Math.random()*.7,.18+Math.random()*.64],false);if(!reduce)requestAnimationFrame(f)}f()}function steam(c){const x=c.getContext('2d'),sx=+(c.dataset.sourceX||.56),sy=+(c.dataset.sourceY||.67);let s=[];const host=c.parentElement;host.addEventListener('pointermove',e=>{const q=pos(c,e);s.forEach(a=>{const dx=a.x-q[0],dy=a.y-q[1],m=dx*dx+dy*dy;if(m<.045)a.vx+=dx*.0012})});function f(){const d=size(c),w=c.width,h=c.height;x.clearRect(0,0,w,h);if(Math.random()<.72)s.push({x:sx+(Math.random()-.5)*.10,y:sy+(Math.random()-.5)*.025,vx:(Math.random()-.5)*.00028,vy:-.00105-Math.random()*.0008,a:.08+Math.random()*.12,r:9+Math.random()*15,phase:Math.random()*6.28});x.globalCompositeOperation='screen';x.filter=`blur(${5*d}px)`;for(const a of s){a.phase+=.025;a.x+=a.vx+Math.sin(a.phase)*.00042;a.y+=a.vy;a.a*=.993;a.r+=.045;const rr=a.r*d,g=x.createRadialGradient(a.x*w,a.y*h,0,a.x*w,a.y*h,rr);g.addColorStop(0,`rgba(245,245,240,${a.a*.58})`);g.addColorStop(.45,`rgba(230,232,230,${a.a*.30})`);g.addColorStop(1,'rgba(255,255,255,0)');x.fillStyle=g;x.beginPath();x.ellipse(a.x*w,a.y*h,rr*.62,rr*1.35,Math.sin(a.phase)*.22,0,7);x.fill()}x.filter='none';s=s.filter(a=>a.y>Math.max(.08,sy-.5)&&a.a>.012).slice(-130);if(!reduce)requestAnimationFrame(f)}f()}canvases.forEach(c=>{c.style.position='absolute';c.style.inset='0';c.style.zIndex='4';c.style.pointerEvents='none';if(c.dataset.effect==='ink')ink(c);if(c.dataset.effect==='steam')steam(c)});window.__tsubasaEffects={isolated:true,effects:canvases.map(c=>c.dataset.effect)};})();