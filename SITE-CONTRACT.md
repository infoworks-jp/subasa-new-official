# 味一番つばさ 新サイト 制作・完成契約

このファイルはサイト制作全体の管理契約。実装者の自己申告では完成にならない。

## 1. 管理原則
- ユーザーの明示指示を最優先し、勝手な簡略化・代替・象徴化・仕様変更を禁止する。
- 「似たもの」「軽量版」「仮実装」を完成品として扱わない。
- 提供された原本素材は原本のまま使用する。変更が必要な場合は別ファイルとし、原本を上書きしない。
- 未実装、未検査、検査FAIL、公開未確認の状態を完成と呼ばない。
- 完成判定は GitHub Actions の全検査PASSと証拠artifactで行う。
- 「できない」「不可能」「制限で実行できない」と判定・報告する場合は、実際に実行して得た失敗ログ、または使用中のツール/サービス仕様に明記された禁止・上限の一次証拠を必須とする。
- 実行していない推測、未確認の上限、経験則、もっともらしい説明だけを根拠に「できない」と報告することを禁止する。
- 上記一次証拠が無い「できない」判定は `UNPROVEN_CANNOT=FAIL` とし、作業停止理由として認めない。
- 実行可能な操作が存在する場合は、推測より先に実行し、その実行結果を証拠として保存する。

## 2. TOP固定仕様
- 背景: すすきの交差点 `assets/susukino-top.webp`。
- 中央: ユーザー提供の縦長「味一番つばさ」SVGロゴを大きく配置。
- 左上の「味一番つばさ」文字ロゴは表示しない。
- TOPは three-fluid-fx / TSL / WebGPU を使用する本物のFluid実装。
- 背景写真そのものと中央ロゴの両方を同一Fluid表現の対象にする。
- マウス/ポインター操作で流体変化が目視できること。
- Canvas2D等で似せただけの代替Fluidは禁止。

## 3. コンテンツ固定事項
- 店名: 味一番つばさ。
- 営業時間: 11:00 — 03:00。
- 定休日: 毎週月曜日。
- 所在地: 札幌市中央区南4条西3丁目1-1 第3グリーンビル 新ラーメン横丁。
- 電話: 011-521-5963。
- 究極の味噌ラーメン: ¥1,100。
- つばさラーメン: ¥2,000。
- 日本語・英語・中国語・韓国語メニューを保持する。

## 4. 素材管理
- 必須素材の欠損・0 byte・破損を禁止。
- ユーザー提供ロゴは簡略化禁止。原本SVGを保存し、500,000 bytes未満への置換をFAILとする。
- 商品写真、店内写真、すすきの交差点、メニュー画像の参照切れをFAILとする。
- 原本のSHA-256を証拠artifactへ記録する。

## 5. 機能検査
- HTML/CSS/JS読み込みエラーなし。
- JavaScript pageerrorなし。
- 404リソースなし。
- ナビゲーション、メニュー拡大、Google Map、電話リンクが存在する。
- PC 1440px と mobile 390px の両方を検査する。
- 横スクロール、重大な重なり、主要要素の画面外消失をFAILとする。
- TOPのpointer操作前後で実ピクセル変化を測定し、変化なしをFAILとする。

## 6. 完成ゲート
完成には以下すべてが必要。
1. CONTRACT_PASS
2. ASSET_INTEGRITY_PASS
3. CONTENT_PASS
4. FUNCTION_PASS
5. DESKTOP_VISUAL_PASS
6. MOBILE_VISUAL_PASS
7. INTERACTION_PASS
8. PUBLIC_SITE_PASS
9. 証拠artifact生成
10. GitHub manager verdict = VERIFIED
11. UNPROVEN_CANNOT が存在しないこと

1つでも欠ければ未完成。

## 7. ブランチ運用
- `work`: 制作・修正用。未完成を許容する。
- `production`: VERIFIED済み成果物だけを置く完成用。
- 検査前の変更を完成扱いしない。

## 8. 報告規則
- 実装者の「できた」「完成した」は完成証拠にならない。
- commit SHA、Actions結果、artifact、公開サイト検査結果を根拠に報告する。
- FAIL時は完成報告を禁止し、FAIL理由をそのまま提示する。
- 「できない」と報告するときは、失敗した実行の識別情報/ログ、または明示的仕様根拠を同時に提示する。証拠なしでは「未検証」と報告し、作業を止めない。
