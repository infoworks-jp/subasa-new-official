(()=>{if(!matchMedia('(max-width:700px)').matches)return;const canvases=[...document.querySelectorAll('.fx-canvas[data-effect="steam"]')];const sims=[];for(const c of canvases){const gl=c.getContext('webgl',{alpha:true,antialias:false,premultipliedAlpha:false})||c.getContext('experimental-webgl');if(!gl)continue;const profile=c.dataset.profile||'miso';const vs=`attribute vec2 p;varying vec2 v;void main(){v=p*.5+.5;gl_Position=vec4(p,0.,1.);}`;const fs=`precision highp float;varying vec2 v;uniform float t;uniform vec2 src;uniform float seed;uniform vec2 pointer;uniform float pointerA;float h(vec2 p){return fract(sin(dot(p,vec2(127.1,311.7)))*43758.5453);}float n(vec2 p){vec2 i=floor(p),f=fract(p);f=f*f*(3.-2.*f);return mix(mix(h(i),h(i+vec2(1.,0.)),f.x),mix(h(i+vec2(0.,1.)),h(i+vec2(1.,1.)),f.x),f.y);}float fbm(vec2 p){float s=0.,a=.5;for(int i=0;i<5;i++){s+=a*n(p);p=p*2.03+vec2(8.7,13.1);a*=.5;}return s;}void main(){vec2 uv=v;float y=uv.y-src.y;if(y<-.02){gl_FragColor=vec4(0.);return;}float rise=clamp(y/.58,0.,1.);float drift=sin(y*12.0+t*1.15+seed)*.022+sin(y*25.0-t*.72+seed*1.9)*.010;float bend=(pointer.x-src.x)*pointerA*.11*exp(-abs(pointer.y-uv.y)*4.0);float x=uv.x-src.x-drift-bend;float w=.018+rise*.085;float core=exp(-(x*x)/(w*w))*smoothstep(-.01,.05,y)*(1.-smoothstep(.38,.68,y));float q=fbm(vec2(x*13.+seed,y*8.-t*.46));float q2=fbm(vec2(x*22.-seed,y*12.+t*.22));float wisps=smoothstep(.40,.78,q*.72+q2*.42);float holes=smoothstep(.22,.60,q2);float steam=core*(.18+1.05*wisps)*(1.-.28*holes);float edge=smoothstep(.02,.16,steam);float a=clamp(steam*.58,0.,.34)*edge;vec3 col=mix(vec3(.78,.81,.83),vec3(.98),clamp(steam*1.8,0.,1.));gl_FragColor=vec4(col,a);}`;function sh(type,src){const s=gl.createShader(type);gl.shaderSource(s,src);gl.compileShader(s);if(!gl.getShaderParameter(s,gl.COMPILE_STATUS))throw Error(gl.getShaderInfoLog(s));return s}const pr=gl.createProgram();gl.attachShader(pr,sh(gl.VERTEX_SHADER,vs));gl.attachShader(pr,sh(gl.FRAGMENT_SHADER,fs));gl.linkProgram(pr);gl.useProgram(pr);const b=gl.createBuffer();gl.bindBuffer(gl.ARRAY_BUFFER,b);gl.bufferData(gl.ARRAY_BUFFER,new Float32Array([-1,-1,3,-1,-1,3]),gl.STATIC_DRAW);const loc=gl.getAttribLocation(pr,'p');gl.enableVertexAttribArray(loc);gl.vertexAttribPointer(loc,2,gl.FLOAT,false,0,0);const source=profile==='miso'?[.73,.39]:profile==='tsubasa-a'?[.18,.42]:[.82,.42],seed=profile==='miso'?1.3:profile==='tsubasa-a'?3.1:6.4;const s={c,gl,pr,source,seed,px:.5,py:.5,tx:.5,ty:.5,a:0,last:0,visible:true};const resize=()=>{const r=c.parentElement.getBoundingClientRect(),dpr=Math.min(devicePixelRatio||1,1.25);c.width=Math.max(1,Math.round(r.width*dpr));c.height=Math.max(1,Math.round(r.height*dpr));c.style.width=r.width+'px';c.style.height=r.height+'px';gl.viewport(0,0,c.width,c.height)};resize();addEventListener('resize',resize,{passive:true});new IntersectionObserver(e=>s.visible=e[0].isIntersecting,{rootMargin:'80px'}).observe(c);const hit=e=>{const r=c.getBoundingClientRect();s.tx=(e.clientX-r.left)/r.width;s.ty=1-(e.clientY-r.top)/r.height;s.a=1;s.last=performance.now()};c.parentElement.addEventListener('pointermove',hit,{passive:true});c.parentElement.addEventListener('pointerdown',hit,{passive:true});sims.push(s)}function frame(now){for(const s of sims){if(!s.visible)continue;s.px+=(s.tx-s.px)*.13;s.py+=(s.ty-s.py)*.13;if(now-s.last>600)s.a*=.96;const gl=s.gl,p=s.pr;gl.useProgram(p);gl.uniform1f(gl.getUniformLocation(p,'t'),now*.001);gl.uniform2f(gl.getUniformLocation(p,'src'),s.source[0],s.source[1]);gl.uniform1f(gl.getUniformLocation(p,'seed'),s.seed);gl.uniform2f(gl.getUniformLocation(p,'pointer'),s.px,s.py);gl.uniform1f(gl.getUniformLocation(p,'pointerA'),s.a);gl.clearColor(0,0,0,0);gl.clear(gl.COLOR_BUFFER_BIT);gl.drawArrays(gl.TRIANGLES,0,3)}requestAnimationFrame(frame)}requestAnimationFrame(frame);window.__tsubasaEffects={engine:'mobile-wispy-webgl-steam',effects:['steam','wispy-flow','pointer-deflection'],interactive:true,canvasCount:sims.length,gpu:true,mobile:true};})();